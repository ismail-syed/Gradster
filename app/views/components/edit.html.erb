<h1>Editing component</h1>

<%= render 'form' %>

<%= link_to "Delete Component", course_component_path(@component.course, @component), 
							  	  		method: :delete, data:{confirm: "Are you sure you want to delete this?"}, :class => 'btn btn-sm btn-danger' %>

</br>
<%= link_to 'Cancel', polymorphic_url(@course) %>
